<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prediction Result</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container mt-5">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h2 class="text-center mb-0">Prediction Result</h2>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6 offset-md-3">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item"><strong>Patient ID:</strong> <%= patientId %></li>
                            <li class="list-group-item"><strong>Risk Score:</strong> <%= prediction.risk_score %></li>
                            <li class="list-group-item"><strong>Risk Class:</strong> <span class="badge <%= prediction.risk_class === 'High' ? 'bg-danger' : 'bg-success' %>"><%= prediction.risk_class %></span></li>
                            <li class="list-group-item"><strong>RiskAssessment ID:</strong> <%= prediction.risk_assessment_id %></li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="card-footer text-center">
                <a href="/" class="btn btn-primary">Return to Home</a>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
